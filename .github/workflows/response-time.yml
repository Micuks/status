name: Response Time CI
on:
  schedule:
    - cron: "0 23 * * *"
  repository_dispatch:
    types: [response_time]
  workflow_dispatch:

jobs:
  release:
    name: Response time graphs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Response Time
        uses: upptime/uptime-monitor@v1.43.11
        with:
          command: "response-time"
        env:
          GH_PAT: ${{ secrets.GH_PAT }}